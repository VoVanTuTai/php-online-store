# Database

Export the local MySQL database and place it here as:

```text
songtai_shop.sql
```

Recommended database name:

```text
songtai_shop
```

After importing the database, copy `config/config.example.php` to `config/config.php` and update the local credentials.

